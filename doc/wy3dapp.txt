---------------------------------------
拾取
---------------------------------------
1.点选支持拾取元素&面&边;框选仅支持拾取元素.
目前元素(例如wy3d::Box)的渲染是将面Batch成一个PrimitiveSet添加到Geometry,这是基于性能的考虑;
拾取面需要将每个面渲染成一个PrimitiveSet,这会影响性能.
目前的做法是点选面时,动态将元素的面渲染成PrimitiveSet.